/* eslint-disable */
import test from 'ava'
import messages from '../messages'
import {render, initialize} from '../test/helpers/render'

test.before(initialize)

test('renders company list', t => {

})