import React from 'react'

export const AccountEdit = React.createClass({
  render () {
    return (
      <h5>You can try and get a new token or something...</h5>
    )
  }
})

export default AccountEdit
